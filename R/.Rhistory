?etable
# load data
data <- fread(file.path(data_dir, "price_wage_panel_monthly.csv.gz"))
i <- 1
for (fe in c("", "| cz_x", "| cz_x^month", "| chain")) {
mdl <- fixest::feols(as.formula(paste0("wage_log ~ price_log", fe)), data = data)
assign(paste0("m", i))
i <- i + 1
}
# load data
data <- fread(file.path(data_dir, "price_wage_panel_monthly.csv.gz"))
i <- 1
for (fe in c("", "| cz_x", "| cz_x^month", "| chain")) {
mdl <- fixest::feols(as.formula(paste0("wage_log ~ price_log", fe)), data = data)
assign(paste0("m", i), mdl)
i <- i + 1
}
data
# load data
data <- fread(file.path(data_dir, "price_wage_panel_monthly.csv.gz"))
i <- 1
for (fe in c("", "| cz", "| cz^month", "| chain")) {
mdl <- fixest::feols(as.formula(paste0("wage_log ~ price_log", fe)), data = data)
assign(paste0("m", i), mdl)
i <- i + 1
}
# set variable names
dict <- c("wage_log"="Log(Wage)",
"price_log"="Log(Price)",
"cz"="Market",
"month" = "Month",
"chain" = "Chain",
)
# set variable names
dict <- c("wage_log"="Log(Wage)",
"price_log"="Log(Price)",
"cz"="Market",
"month" = "Month",
"chain" = "Chain"
)
fixest::setFixest_dict(dict)
#### To LaTeX and beyond!
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i))), fitstat = ~ n + my + ar2 + fe
depvar = FALSE,
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i))), fitstat = ~ n + my + ar2 + fe,
depvar = FALSE,
tex = TRUE)
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2 + fe,
depvar = FALSE,
tex = TRUE)
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2 + fe,
depvar = FALSE,
tex = TRUE)
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2,
depvar = FALSE,
tex = TRUE)
a
?etable
tabs
cat(a, file = file.path(tabs, 'wages_prices_reg.tex'))
cat(a, file = file.path(tabs, 'wages_prices_reg.tex'))
results_dir <- "/Users/davidvandijcke/Dropbox (University of Michigan)/Apps/Overleaf/dynamic/results"
tabs <- file.path(results_dir, "tabs")
figs <- file.path(results_dir, "figs")
cat(a, file = file.path(tabs, 'wages_prices_reg.tex'))
#-------
# Relation between wages, prices, and distance
#-------
# load data
data <- fread(file.path(data_dir, "price_wage_panel_monthly.csv.gz"))
#-------
# Relation between wages, prices, and distance
#-------
# load data
data <- fread(file.path(data_dir, "price_wage_panel_distances.csv.gz"))
#-------
# Relation between wages, prices, and distance
#-------
# load data
data <- fread(file.path(data_dir, "price_wage_panel_distances.csv.gz"))
data
# set variable names
dict <- c("wage_log"="Log(Wage)",
"price_log"="Log(Price)",
"cz"="Market",
"distance_shoppers_log" = "Log(Dist. to Shoppers)",
"distance_workers_log" = "Log(Dist. to Workers)",
"month" = "Month",
"chain" = "Chain"
)
fixest::setFixest_dict(dict)
mdl <- fixest::feols(as.formula("distance_shoppers_log ~ distance_workers_log"), data = data)
summary(mdl)
# load data
data <- fread(file.path(data_dir, "price_wage_panel_monthly.csv.gz"))
i <- 1
for (fe in c("", "| cz", "| cz^month", "| chain")) {
mdl <- fixest::feols(as.formula(paste0("wage_log ~ price_log", fe)), data = data)
assign(paste0("m", i), mdl)
i <- i + 1
}
#### To LaTeX and beyond!
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2,
depvar = TRUE,
tex = TRUE)
cat(a, file = file.path(tabs, 'wages_prices_reg.tex'))
tabs
a
cat(a, file = file.path(tabs, 'wages_prices_reg.tex'))
paste0("1, "2", "3")
paste0(c("1, "2", "3"))
paste0("1", "2", "3")
data <- fread(file.path(data_dir, "price_wage_panel_distances.csv.gz"))
mdl <- fixest::feols(as.formula("distance_shoppers_log ~ distance_workers_log"), data = data)
summary(mdl)
i <- 1
for (fe in c("", "| cz", "| chain")) {
for (yvar in c("wage_log", "price_log")) {
if (yvar == "wage_log") { xvar <- "distance_workers_log" } else { xvar <- "distance_shoppers_log" }
mdl <- fixest::feols(as.formula(paste0(yvar, "~", xvar, fe)), data = data)
assign(paste0("m", i), mdl)
i <- i + 1
}
}
#### To LaTeX and beyond!
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2,
depvar = FALSE,
tex = TRUE)
cat(a, file = file.path(tabs, 'wages_prices_location_reg.tex'))
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2,
depvar = TRUE,
tex = TRUE)
cat(a, file = file.path(tabs, 'wages_prices_location_reg.tex'))
m1
m2
i <- 1
for (yvar in c("wage_log", "price_log")) {
for (fe in c("", "| cz", "| chain")) {
if (yvar == "wage_log") { xvar <- "distance_workers_log" } else { xvar <- "distance_shoppers_log" }
mdl <- fixest::feols(as.formula(paste0(yvar, "~", xvar, fe)), data = data)
assign(paste0("m", i), mdl)
i <- i + 1
}
}
#### To LaTeX and beyond!
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2,
depvar = TRUE,
tex = TRUE)
cat(a, file = file.path(tabs, 'wages_prices_location_reg.tex'))
data <- fread(file.path(data_dir, "price_wage_panel_distances.csv.gz"))
mdl <- fixest::feols(as.formula("distance_shoppers_log ~ distance_workers_log"), data = data)
summary(mdl)
i <- 1
for (yvar in c("wage_log", "price_log")) {
for (fe in c("", "| cz", "| chain")) {
if (yvar == "wage_log") { xvar <- "distance_workers" } else { xvar <- "distance_shoppers_log" }
mdl <- fixest::feols(as.formula(paste0(yvar, "~", xvar, fe)), data = data)
assign(paste0("m", i), mdl)
i <- i + 1
}
}
#### To LaTeX and beyond!
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2,
depvar = TRUE,
tex = TRUE)
a
#-------
# Relation between wages, prices, and distance
#-------
# load data
data <- fread(file.path(data_dir, "price_wage_panel_distances.csv.gz"))
mdl <- fixest::feols(as.formula("distance_shoppers_log ~ distance_workers_log"), data = data)
summary(mdl)
i <- 1
for (yvar in c("wage_log", "price_log")) {
for (fe in c("", "| cz", "| chain")) {
if (yvar == "wage_log") { xvar <- "distance_workers + distance_workers_sq" } else { xvar <- "distance_shoppers_log" }
mdl <- fixest::feols(as.formula(paste0(yvar, "~", xvar, fe)), data = data)
assign(paste0("m", i), mdl)
i <- i + 1
}
}
#### To LaTeX and beyond!
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2,
depvar = TRUE,
tex = TRUE)
a
data <- fread(file.path(data_dir, "price_wage_panel_distances.csv.gz"))
mdl <- fixest::feols(as.formula("distance_shoppers_log ~ distance_workers_log"), data = data)
summary(mdl)
i <- 1
for (yvar in c("wage_log", "price_log")) {
for (fe in c("", "| cz", "| chain")) {
if (yvar == "wage_log") { xvar <- "distance_workers" } else { xvar <- "distance_shoppers_log" }
mdl <- fixest::feols(as.formula(paste0(yvar, "~", xvar, fe)), data = data)
assign(paste0("m", i), mdl)
i <- i + 1
}
}
#### To LaTeX and beyond!
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2,
depvar = TRUE,
tex = TRUE)
a
i <- 1
for (yvar in c("wage_log", "price_log")) {
for (fe in c("", "| cz", "| chain")) {
if (yvar == "wage_log") { xvar <- "distance_workers + distance_workers_sq" } else { xvar <- "distance_shoppers_log" }
mdl <- fixest::feols(as.formula(paste0(yvar, "~", xvar, fe)), data = data)
assign(paste0("m", i), mdl)
i <- i + 1
}
}
#### To LaTeX and beyond!
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2,
depvar = TRUE,
tex = TRUE)
a
summary(data$distance_workers)
i <- 1
for (yvar in c("wage_log", "price_log")) {
for (fe in c("", "| cz", "| chain")) {
if (yvar == "wage_log") { xvar <- "distance_workers + distance_workers_sq" } else { xvar <- "distance_shoppers + distance_shoppers_sq" }
mdl <- fixest::feols(as.formula(paste0(yvar, "~", xvar, fe)), data = data)
assign(paste0("m", i), mdl)
i <- i + 1
}
}
#### To LaTeX and beyond!
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2,
depvar = TRUE,
tex = TRUE)
a
# set variable names
dict <- c("wage_log"="Log(Wage)",
"price_log"="Log(Price)",
"cz"="Market",
"distance_shoppers_log" = "Log(Dist. to Shoppers)",
"distance_shoppers" = "Dist. to Shoppers",
"distance_shoppers_sq" = "Dist. to Shoppers^2",
"distance_workers_log" = "Log(Dist. to Workers)",
"distance_workers" = "Dist. to Workers",
"distance_workers_sq" = "Dist. to Workers^2",
"month" = "Month",
"chain" = "Chain"
)
fixest::setFixest_dict(dict)
# load data
data <- fread(file.path(data_dir, "price_wage_panel_distances.csv.gz"))
mdl <- fixest::feols(as.formula("distance_shoppers_log ~ distance_workers_log"), data = data)
summary(mdl)
i <- 1
for (yvar in c("wage_log", "price_log")) {
for (fe in c("", "| cz", "| chain")) {
if (yvar == "wage_log") { xvar <- "distance_workers + distance_workers_sq" } else { xvar <- "distance_shoppers + distance_shoppers_sq" }
mdl <- fixest::feols(as.formula(paste0(yvar, "~", xvar, fe)), data = data)
assign(paste0("m", i), mdl)
i <- i + 1
}
}
#### To LaTeX and beyond!
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2,
depvar = TRUE,
tex = TRUE)
cat(a, file = file.path(tabs, 'wages_prices_location_reg.tex'))
mdl <- fixest::feols(as.formula("distance_shoppers_log ~ distance_workers_log"), data = data)
summary(mdl)
i <- 1
for (yvar in c("wage_log", "price_log")) {
for (fe in c("", "| cz", "| chain")) {
if (yvar == "wage_log") { xvar <- "distance_workers_log" } else { xvar <- "distance_shoppers_log" }
mdl <- fixest::feols(as.formula(paste0(yvar, "~", xvar, fe)), data = data)
assign(paste0("m", i), mdl)
i <- i + 1
}
}
#### To LaTeX and beyond!
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2,
depvar = TRUE,
tex = TRUE)
cat(a, file = file.path(tabs, 'wages_prices_location_reg.tex'))
?etable
#-------
# Relation between wages, prices, and distance
#-------
# load data
data <- fread(file.path(data_dir, "price_wage_panel_distances.csv.gz"))
mdl <- fixest::feols(as.formula("distance_shoppers_log ~ distance_workers_log"), data = data)
summary(mdl)
i <- 1
for (yvar in c("wage_log", "price_log")) {
for (fe in c("", "| cz", "| chain")) {
if (yvar == "wage_log") { xvar <- "distance_workers + distance_workers_sq" } else { xvar <- "distance_shoppers + distance_shoppers_sq" }
mdl <- fixest::feols(as.formula(paste0(yvar, "~", xvar, fe)), data = data)
assign(paste0("m", i), mdl)
i <- i + 1
}
}
#### To LaTeX and beyond!
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2,
depvar = TRUE,
tex = TRUE)
cat(a, file = file.path(tabs, 'wages_prices_location_reg.tex'))
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2,
depvar = TRUE,
tex = TRUE, digits=5)
cat(a, file = file.path(tabs, 'wages_prices_location_reg.tex'))
a
#### To LaTeX and beyond!
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2,
depvar = TRUE,
tex = TRUE, digits='r5')
a
#### To LaTeX and beyond!
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2,
depvar = TRUE,
tex = TRUE, digits='r5')
cat(a, file = file.path(tabs, 'wages_prices_location_reg.tex'))
# set variable names
dict <- c("wage_log"="Log(Wage)",
"price_log"="Log(Price)",
"cz"="Market",
"distance_shoppers_log" = "Log(Dist. to Shoppers)",
"distance_shoppers" = "Dist. to Shoppers",
"distance_shoppers_sq" = "Dist. to Shoppers^2",
"distance_workers_log" = "Log(Dist. to Workers)",
"distance_workers" = "Dist. to Workers",
"distance_workers_sq" = "Dist. to Workers^2",
"month" = "Month",
"chain" = "Chain"
)
fixest::setFixest_dict(dict)
# load data
data <- fread(file.path(data_dir, "price_wage_panel_monthly.csv.gz"))
i <- 1
for (fe in c("", "| cz", "| cz^month", "| chain")) {
mdl <- fixest::feols(as.formula(paste0("wage_log ~ price_log", fe)), data = data)
assign(paste0("m", i), mdl)
i <- i + 1
}
#### To LaTeX and beyond!
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2,
depvar = TRUE,
tex = TRUE, digits='r5')
cat(a, file = file.path(tabs, 'wages_prices_reg.tex'))
#-------
# Relation between wages and prices
#-------
# set variable names
dict <- c("wage_log"="Log(Wage)",
"price_log"="Log(Price)",
"cz"="Market",
"distance_shoppers_log" = "Log(Dist. to Shoppers)",
"distance_shoppers" = "Dist. to Shoppers",
"distance_shoppers_sq" = "Dist. to Shoppers$^$2",
"distance_workers_log" = "Log(Dist. to Workers)",
"distance_workers" = "Dist. to Workers",
"distance_workers_sq" = "Dist. to Workers$^$2",
"month" = "Month",
"chain" = "Chain"
)
fixest::setFixest_dict(dict)
# load data
data <- fread(file.path(data_dir, "price_wage_panel_monthly.csv.gz"))
i <- 1
for (fe in c("", "| cz", "| cz^month", "| chain")) {
mdl <- fixest::feols(as.formula(paste0("wage_log ~ price_log", fe)), data = data)
assign(paste0("m", i), mdl)
i <- i + 1
}
#### To LaTeX and beyond!
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2,
depvar = TRUE,
tex = TRUE, digits='r4')
cat(a, file = file.path(tabs, 'wages_prices_reg.tex'))
#-------
# Relation between wages, prices, and distance
#-------
# load data
data <- fread(file.path(data_dir, "price_wage_panel_distances.csv.gz"))
mdl <- fixest::feols(as.formula("distance_shoppers_log ~ distance_workers_log"), data = data)
summary(mdl)
i <- 1
for (yvar in c("wage_log", "price_log")) {
for (fe in c("", "| cz", "| chain")) {
if (yvar == "wage_log") { xvar <- "distance_workers + distance_workers_sq" } else { xvar <- "distance_shoppers + distance_shoppers_sq" }
mdl <- fixest::feols(as.formula(paste0(yvar, "~", xvar, fe)), data = data)
assign(paste0("m", i), mdl)
i <- i + 1
}
}
#### To LaTeX and beyond!
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2,
depvar = TRUE,
tex = TRUE, digits='r4')
cat(a, file = file.path(tabs, 'wages_prices_location_reg.tex'))
#-------
# Relation between wages and prices
#-------
# set variable names
dict <- c("wage_log"="Log(Wage)",
"price_log"="Log(Price)",
"cz"="Market",
"distance_shoppers_log" = "Log(Dist. to Shoppers)",
"distance_shoppers" = "Dist. to Shoppers",
"distance_shoppers_sq" = "Dist. to Shoppers$^2$",
"distance_workers_log" = "Log(Dist. to Workers)",
"distance_workers" = "Dist. to Workers",
"distance_workers_sq" = "Dist. to Workers$^2$",
"month" = "Month",
"chain" = "Chain"
)
fixest::setFixest_dict(dict)
# load data
data <- fread(file.path(data_dir, "price_wage_panel_monthly.csv.gz"))
i <- 1
for (fe in c("", "| cz", "| cz^month", "| chain")) {
mdl <- fixest::feols(as.formula(paste0("wage_log ~ price_log", fe)), data = data)
assign(paste0("m", i), mdl)
i <- i + 1
}
#### To LaTeX and beyond!
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2,
depvar = TRUE,
tex = TRUE, digits='r4')
cat(a, file = file.path(tabs, 'wages_prices_reg.tex'))
#-------
# Relation between wages, prices, and distance
#-------
# load data
data <- fread(file.path(data_dir, "price_wage_panel_distances.csv.gz"))
mdl <- fixest::feols(as.formula("distance_shoppers_log ~ distance_workers_log"), data = data)
summary(mdl)
i <- 1
for (yvar in c("wage_log", "price_log")) {
for (fe in c("", "| cz", "| chain")) {
if (yvar == "wage_log") { xvar <- "distance_workers + distance_workers_sq" } else { xvar <- "distance_shoppers + distance_shoppers_sq" }
mdl <- fixest::feols(as.formula(paste0(yvar, "~", xvar, fe)), data = data)
assign(paste0("m", i), mdl)
i <- i + 1
}
}
#### To LaTeX and beyond!
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2,
depvar = TRUE,
tex = TRUE, digits='r4')
cat(a, file = file.path(tabs, 'wages_prices_location_reg.tex'))
mdl <- fixest::feols(as.formula("distance_shoppers_log ~ distance_workers_log"), data = data)
summary(mdl)
mdl <- fixest::feols(as.formula("distance_shoppers_log ~ distance_workers + distance_workers_sq"), data = data)
summary(mdl)
mdl <- fixest::feols(as.formula("distance_shoppers ~ distance_workers + distance_workers_sq"), data = data)
summary(mdl)
summary(data$distance_workers)
quantile(data$distance_workers, 0.95)
quantile(data$distance_workers, 0.95, na.rm=TRUE)
quantile(data$distance_shoppers, 0.95, na.rm=TRUE)
quantile(data$distance_workers, 0.99)
quantile(data$distance_workers, 0.99, na.rm=TRUE)
# load data
data <- fread(file.path(data_dir, "price_wage_panel_distances.csv.gz"))
mdl <- fixest::feols(as.formula("distance_shoppers ~ distance_workers + distance_workers_sq"), data = data)
summary(mdl)
mdl <- fixest::feols(as.formula("distance_shoppers ~ distance_workers + distance_workers_sq | chain"), data = data)
summary(mdl)
mdl <- fixest::feols(as.formula("distance_workers ~ distance_shoppers + distance_shoppers_sq"), data = data)
summary(mdl)
# load data
data <- fread(file.path(data_dir, "price_wage_panel_distances.csv.gz"))
i <- 1
for (fe in c("", "| cz", "| cz^month", "| chain")) {
mdl <- fixest::feols(as.formula(paste0("distance_shoppers ~ distance_workers + distance_workers_sq", fe)), data = data)
assign(paste0("m", i), mdl)
i <- i + 1
}
i <- 1
for (fe in c("", "| cz", "| chain")) {
mdl <- fixest::feols(as.formula(paste0("distance_shoppers ~ distance_workers + distance_workers_sq", fe)), data = data)
assign(paste0("m", i), mdl)
i <- i + 1
}
#### To LaTeX and beyond!
# create latex table: part a
a <- etable(mget(paste0("m", seq(1,i-1))), fitstat = ~ n + my + ar2,
depvar = TRUE,
tex = TRUE, digits='r4')
cat(a, file = file.path(tabs, 'distances_reg.tex'))
